<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
    <typeAliases>
        <!--定义别名:typeAlias-->
        <!--批量定义别名,批量指定很简单,只要指定包名即可,之后程序会为包下的所有类都自动加上别名,
        其定义别名的规范就是对应包装类的类名首字母变为小写-->
        <package name="com.wj.ssm.entity.User"/>
    </typeAliases>

   <!-- 在此不需要的原因是,我们在Spring中使用了c3p0的连接池对象
         <environments default="development">
         <environment id="development">
         <transactionManager type="JDBC"></transactionManager>
         <dataSource type="POOLED">
         <property name="driver" value="com.mysql.jdbc.Driver"/>
         <property name="url" value="jdbc:mysql://localhost:3306/up2?characterEncoding=utf8"/>
         <property name="username" value="root"/>
         <property name="password" value="root"/>
         </dataSource>
         </environment>
         </environments>-->

    <mappers>
        <!--不需要的原因是,Spring中使用了mapper扫描器
        <package name="com.uplooking.ssm.mapper"></package>-->
    </mappers>
</configuration>
        <!--mybatis的配置文件-->